local TweenService = game:GetService("TweenService")

local Elements = {}

function Elements.AddToggle(parent, side, text, callback)
    -- код Toggle
end

function Elements.AddButton(parent, side, text, callback)
    -- код Button
end

return Elements
