local TweenService = game:GetService("TweenService")

local Tabs = {}

function Tabs.CreateTab(WindowContext, name, emoji)
    -- логика вкладок (Pages, Sidebar и т.п.)
    return TabLogic
end

return Tabs
